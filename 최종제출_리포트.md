# 국내 여행 추천 프로그램 — 최종 제출 리포트

**개발자**: 준
**개발 기간**: 2026년 8월
**개발 도구**: Claude Code (AI 페어 프로그래밍)
**사용 API**: Google Gemini (LLM) + Kakao Local (지도/장소 검색)

---

## 1. 프로젝트 개요

Python CLI 프로그램으로, 사용자가 여행 날짜를 입력하면:

1. **Gemini API**가 해당 시기에 여행하기 좋은 국내 지역을 추천하고
2. **Kakao Local API**로 그 지역의 맛집 정보를 검색한 뒤
3. 두 결과를 종합해 **Gemini API**가 최종 여행 리포트(Markdown)를 생성한다

단일 API 호출이 아니라, 여러 API의 응답을 서로 연결해 하나의 인사이트를 만들어내는 파이프라인 구조로 설계했다.

---

## 2. 실행 방법

```bash
# 1. 가상환경 생성 및 패키지 설치
python3 -m venv venv
venv/bin/pip install -r requirements.txt

# 2. .env 파일에 API 키 설정
# GEMINI_API_KEY=your_key
# KAKAO_API_KEY=your_key

# 3. 실행
venv/bin/python travel_planner.py --date "2026-09-02"
```

실행하면 아래와 같이 진행 로그가 출력되고, `results/` 폴더에 결과가 저장된다.

```
[1/3] 1차 추천 생성 중(LLM)...
  - recommended_city: "평창"
[2/3] 맛집 검색 중(지도/장소 API)...
  - 맛집 5곳 검색 완료
[3/3] 최종 리포트 생성 중(LLM)...
  - 리포트 생성 완료

완료! results/2026-09-02_travel_plan.md 를 확인하세요.
```

---

## 3. 실행 결과물

### 3-1. 원본 데이터 JSON (`results/2026-09-02_travel_data.json`)

```json
{
  "date": "2026-09-02",
  "recommendation": {
    "recommended_city": "평창",
    "weather": "한낮에는 쾌적하고 아침저녁으로는 선선하여 초가을의 정취를 느끼기 좋은 날씨입니다.",
    "events": [
      "평창 효석문화제 (봉평 메밀꽃 축제)",
      "오대산 월정사 전나무숲길 힐링 걷기"
    ],
    "reason": "9월 초 평창 봉평 일대는 소금을 뿌려놓은 듯 하얗게 만개하는 메밀꽃 군락이 장관을 이루는 시기입니다. 대관령 양떼목장과 오대산 등 청정한 고원 지대에서 늦더위를 피해 선선한 바람을 맞으며 휴식할 수 있습니다. 아름다운 자연경관과 함께 가을의 시작을 여유롭게 맞이하기에 최적의 여행지입니다."
  },
  "restaurants": [
    {
      "name": "부일식당",
      "address": "강원특별자치도 평창군 진부면 진부중앙로 100-5",
      "category": "음식점 > 한식",
      "url": "http://place.map.kakao.com/16107216"
    },
    {
      "name": "흔들바위",
      "address": "강원특별자치도 평창군 봉평면 태기로 1",
      "category": "음식점 > 한식 > 한정식",
      "url": "http://place.map.kakao.com/16104219"
    }
  ],
  "errors": []
}
```

### 3-2. 최종 여행 리포트 (`results/2026-09-02_travel_plan.md`)

과제 요구 형식(추천지역/추천이유/날씨/행사/맛집/1일 일정/오류 요약)에 맞춰 자동 생성됨을 확인했다.

---

## 4. 기능 요구사항 구현 확인

| 요구사항 | 구현 여부 |
|---|---|
| argparse 기반 CLI, `--date` 필수 옵션 | ✅ |
| 날짜 형식 검증 (잘못된 형식 → 안내 후 종료) | ✅ |
| Gemini API로 1차 추천 (JSON 스키마 준수) | ✅ |
| Kakao Local API로 맛집 검색 (5곳) | ✅ |
| 검색 0건/실패 시에도 중단 없이 진행 | ✅ |
| Gemini로 최종 마크다운 리포트 생성 | ✅ |
| try-except 기반 에러 처리 | ✅ |
| LLM 파싱 실패 시 1회 재시도 | ✅ |
| API 키 `.env` 관리, 코드에 미포함 | ✅ |
| `results/` 폴더에 JSON + MD 저장 | ✅ |

---

## 5. 실제 발생한 오류와 대응 (실전 검증)

개발 및 테스트 과정에서 아래 오류들을 실제로 겪었고, 프로그램이 요구사항대로 정상 대응하는 것을 확인했다.

| 오류 | 상황 | 프로그램 대응 | 결과 |
|---|---|---|---|
| **Kakao 403 (NotAuthorizedError)** | Kakao 앱에서 Maps 서비스가 비활성화 상태였음 | 원인 진단 후 앱 설정에서 Maps 활성화로 해결 | 이후 정상 검색 |
| **Gemini 503 (UNAVAILABLE)** | Gemini 서버 일시 과부하 | 1회 재시도 로직 작동, 실패 시 fallback 값(`weather: "정보 없음"` 등)으로 대체하고 프로그램은 계속 진행 | 맛집 검색 등 나머지 단계는 정상 완료, `errors` 배열에 기록됨 |
| **잘못된 날짜 형식 입력** (`2026-13-40`, `not-a-date`) | 의도적 테스트 | 즉시 사용법 안내 후 정상 종료 | 요구사항대로 작동 |
| **API 키 미설정** | 초기 테스트 시 `.env` 비어있는 상태 | 즉시 종료 + 설정 방법 안내 출력 | 요구사항대로 작동 |

---

## 6. 과제 목표 이해도 — 질문별 답변

### Q1. REST API의 요청/응답 구조와 GET/POST의 차이는?

REST API는 클라이언트(내 프로그램)가 서버(Gemini, Kakao)에 **요청(Request)**을 보내면, 서버가 **응답(Response)**을 돌려주는 방식으로 동작한다. 요청에는 URL, HTTP 메서드, 헤더(인증 정보), 필요 시 본문(body)이 포함되고, 응답에는 상태 코드(200, 401, 403, 429, 503 등)와 본문(대부분 JSON)이 담긴다.

- **GET**: 데이터를 조회할 때 사용하며 파라미터를 URL에 실어 보낸다. Kakao Local API로 맛집을 검색할 때 검색어를 URL 파라미터로 전달했다.
- **POST**: 데이터를 생성하거나 본문에 담아 보낼 때 사용한다. Gemini API에 프롬프트를 보내 추천을 요청할 때 본문(body)에 프롬프트 내용을 담아 POST로 전송했다.

실제로 Kakao API는 인증 정보를 URL이 아니라 `Authorization: KakaoAK {키}` 헤더에 실어야 했는데, 이 과정에서 앱의 Maps 서비스가 꺼져 있어 403 오류를 겪으며 "인증 실패"와 "권한 거부"가 다른 문제라는 것을 직접 구분해볼 수 있었다.

### Q2. LLM 출력(JSON)을 다음 단계 입력으로 활용하는 흐름은?

이 프로그램의 핵심은 3단계 파이프라인이다.

```
[1단계] Gemini에 날짜를 주고 추천 요청
  → {"recommended_city": "평창", "weather": ..., "events": [...], "reason": ...}

[2단계] 1단계의 recommended_city 값을 그대로
  Kakao Local API의 검색어(query)로 사용
  → 맛집 리스트 획득

[3단계] 1단계 JSON + 2단계 맛집 리스트를 합쳐
  다시 Gemini에 "최종 리포트를 마크다운으로 작성해줘" 요청
  → 완성된 .md 리포트
```

이 흐름에서 중요한 것은 LLM이 자유 텍스트가 아니라 **정해진 키를 가진 JSON**으로 답하도록 프롬프트를 설계해야, 다음 단계 코드가 그 값을 안정적으로 추출해 쓸 수 있다는 점이다. 만약 JSON이 아닌 일반 문장으로 응답했다면 `recommended_city` 값을 코드로 파싱할 수 없어 파이프라인이 끊겼을 것이다.

### Q3. 외부 API 호출의 대표 오류와 대응 원칙은?

프로젝트 진행 중 아래 오류를 실제로 모두 겪었다.

- **인증 오류(401/403)**: Kakao 앱의 Maps 서비스 비활성화로 403 발생 → 콘솔에서 서비스 활성화로 해결. 키 자체 문제와 권한/설정 문제를 구분해서 점검해야 함을 체감했다.
- **쿼터/과부하 오류(429/503)**: Gemini 호출 중 서버 과부하로 503 발생 → 즉시 실패 처리하지 않고 재시도 로직으로 대응했고, 재시도로 정상 응답을 받아 리포트를 완성한 사례도 있었다.
- **파싱 오류**: LLM이 JSON 형식을 어길 경우 "필수 키만 다시 JSON으로 출력해줘"처럼 프롬프트를 강화해 1회만 재시도한다(무한 재시도 금지).
- **공통 원칙**: 어떤 단계에서 오류가 나도 프로그램 전체가 중단되지 않고, "데이터 없음" 또는 기본값으로 대체 처리한 뒤 다음 단계로 진행한다. 어떤 오류가 있었는지는 최종 결과물의 `errors` 섹션에 투명하게 기록한다.

### Q4. API 키를 `.env`/환경변수로 관리하는 이유는?

- **금전적 사고 예방**: 키가 노출되면 타인이 그 키로 유료 API를 무단 호출할 수 있다. 개발 중 실제로 API 키가 대화창에 그대로 노출되는 사고가 있었고, 즉시 해당 키(Gemini, Kakao)를 각 콘솔에서 폐기·재발급해야 했다.
- **코드와 설정의 분리**: 키를 코드에 직접 쓰면 코드 공유 시 키까지 함께 노출될 위험이 크다. `.env`에 키를 분리하고 `.gitignore`에 등록하면 코드는 공유해도 키는 노출되지 않는다.
- **운영 편의성**: 키가 만료되거나 재발급이 필요할 때 코드 수정 없이 `.env` 값만 교체하면 된다.

이 원칙은 이론으로만 배운 것이 아니라, 개발 중 실제로 키 노출 사고를 겪고 재발급까지 대응하며 체득했다.

---

## 7. 보안 조치 사항

- API 키는 코드/README/결과 파일 어디에도 직접 작성하지 않았다.
- `.env` 파일로만 키를 관리했으며 `.gitignore`에 등록해 커밋 방지 처리했다.
- 개발 중 키 노출 사고 발생 시 즉시 재발급하여 대응했다.

## 8. 알려진 제약 사항

- 단일 지역 추천만 지원 (보너스 과제인 복수 지역 추천은 미구현, 시간 관계상 MVP 우선 완성에 집중)
- 같은 날짜로 재실행해도 매번 API를 새로 호출 (결과 캐싱 미구현)

---

## 9. 결론

과제에서 요구한 3가지 결과물(CLI 프로그램, 결과 데이터, README)을 모두 완성했으며, 실제 개발 과정에서 인증 오류(403), 서버 과부하(503), 키 노출 사고 등을 직접 겪으며 API 연동의 실무적 이슈들을 체험적으로 학습했다. 특히 에러 처리 요구사항은 의도적 테스트뿐 아니라 실제 발생한 오류 상황에서도 정상 작동함을 확인했다.
